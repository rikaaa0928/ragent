- [x] 项目级别配置
  - [x] 当前目录下的 .ragent/config.toml也作为配置文件，里面为针对项目的增量配置，项目配置按最子节点key覆盖全局配置中的值。相当于同样的配置项优先读项目配置，全局配置为fallback
  - [x] 项目级别配置中对扩展列表的操作只允许修改enabled和config字段，项目级别配置的扩展列表中只能出现name，enabled和config相关参数，而且不允许出现全局配置中不存在的name。扩展的顺序也沿用全局配置中的顺序，不受项目级别配置影响。全局配置和项目配置中都不允许重名扩展
- [ ] 显示优化
  - [ ] 对外输出的轮次计算中，如果当前会话是继续之前的session，则把轮次数字改成 ${总轮次} (${session历史轮次}+${当前轮次})的表达方法，比如 --- [Agent 轮次 43] 模型思考/回复中 ---改成 --- [Agent 轮次 43 (42+1)] 模型思考/回复中 ---
  - [ ] 支持每次对话的token总量和详细用量的统计和展示,包括输出，输出，缓存，思考的用量
- [ ] 支持-m/--model覆盖指定模型名
- [ ] 每个session启动时都新建一个/tmp/ragent/${session_name}的session_tmp临时目录，用来放置那些不适合出现在workspace的文件，比如从剪切板粘贴的图片，测试代码等过渡内容
  - [ ] session_tmp临时目录的信息和使用指南放在system prompt里
- [ ] 更完善的权限管理
  - [ ] 设计实现权限管理，询问交互系统和交互方式
  - [ ] 支持文件，网络，命令执行目录，三方面权限管理
    - [ ] 命令执行新增执行目录（work dir）参数，默认当前目录，对可访问目录进行权限限制（暂不考虑命令里使用cd，或使用绝对路径等绕过限制的情况，只做简单的参数校验）
    - [ ] 扩展使用wasip2进行网络调用，暂时只添加http支持
    - [ ] 扩展使用wasip2进行文件访问
  - [ ] 全部限制使用黑白名单机制。黑名单直接拒绝，白名单直接通过，其余的看配置，通过，拒绝或询问
    - [ ] 黑白名单放在配置文件里，分为全局配置和扩展配置，扩展配置覆盖全局配置。没有扩展配置时（none为没有配置，注意区分none和空值）使用全局配置
    - [ ] 黑白名单除了全局配置文件和项目配置文件外还支持session级别的配置，放在meta里，优先级为 session,项目，全局，依次fallback
  - [ ] 询问时支持拒绝，本次同意，本session同意，本项目同意，和始终同意。后三个分别把配置落到 session,项目和全局配置中 
  - [ ] 项目级别配置的新建和初次读取需要人工信任
    - [ ] 全局配置里增加一个trusted.toml，里面记录所有信任过的目录，没有信任过的目录启动时需要人工信任，信任时提醒当前目录是否存在存量项目级别配置，提醒用户检查确认内容。
    - [ ] 不存在存量项目级别配置时自动初始化一个，里面自动添加对于当前目录的文件读写和命令执行目录的白名单。
  - [ ] 新session启动时自动添加 session_tmp 目录的文件读写和命令执行目录的白名单。需要先解析已有配置，再添加session配置后写入到session的meta中
- [ ] skill支持
